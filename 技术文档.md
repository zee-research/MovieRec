# MovieRec 技术文档

> 基于知识图谱的个性化电影推荐系统

---

## 一、项目概述

MovieRec 是一个融合**知识图谱**与**协同过滤**的电影推荐系统，采用前后端分离架构。系统以 Neo4j 图数据库为核心存储，构建包含用户、电影、类型三类实体的知识图谱，并在此基础上实现了四种推荐算法的融合推荐。

数据集使用 [MovieLens Latest Small](https://grouplens.org/datasets/movielens/)，包含约 610 位用户对 9,742 部电影的 100,836 条评分记录。

---

## 二、目录结构

```
MovieRec/
├── AGENTS.md                  # AI 编码代理配置
├── README.md                  # 项目说明
├── sql/                       # 数据库 Schema
│   └── neo4j_schema.cypher    # Neo4j Cypher DDL & 示例查询
│
├── backend/                   # Flask 后端服务
│   ├── run.py                 # 启动入口
│   ├── requirements.txt       # Python 依赖清单
│   ├── app/
│   │   ├── __init__.py        # Flask 应用工厂（create_app）
│   │   ├── config.py          # 系统配置（Neo4j、JWT、路径等）
│   │   ├── models/            # 数据模型（预留）
│   │   ├── routes/            # 路由蓝图
│   │   │   ├── __init__.py    # 路由注册中心
│   │   │   ├── user.py        # 用户路由（注册/登录/信息）
│   │   │   ├── movie.py       # 电影路由（列表/详情/类型/热门）
│   │   │   ├── rating.py      # 评分路由（添加/查询/删除）
│   │   │   ├── recommend.py   # 推荐路由（CF/SVD/KG/混合）
│   │   │   ├── graph.py       # 知识图谱可视化路由
│   │   │   └── admin.py       # 管理员路由（用户/电影/评分管理/评估）
│   │   ├── services/          # 业务服务层
│   │   │   ├── neo4j_service.py     # Neo4j 数据访问层（CRUD + 图查询）
│   │   │   └── recommend_service.py # 推荐算法实现（CF/SVD/KG/混合/评估）
│   │   └── utils/
│   │       └── auth.py        # JWT 认证 & 密码加密
│   ├── data/
│   │   ├── ml-latest-small/   # MovieLens 原始数据集
│   │   │   ├── movies.csv     # 电影（movieId, title, genres）
│   │   │   ├── ratings.csv    # 评分（userId, movieId, rating, timestamp）
│   │   │   ├── tags.csv       # 标签
│   │   │   └── links.csv      # 外部 ID 映射（imdbId, tmdbId）
│   │   └── processed/         # 预处理后数据
│   │       ├── movies_cleaned.csv   # 清洗后电影（+clean_title, year）
│   │       ├── ratings_cleaned.csv  # 清洗后评分（+rating_normalized, date_str）
│   │       ├── genres.csv           # 类型字典（genreId, genreName）
│   │       ├── movie_genre.csv      # 电影-类型映射表
│   │       ├── tags_cleaned.csv     # 清洗后标签
│   │       ├── train.csv            # 训练集（80%）
│   │       └── test.csv             # 测试集（20%）
│   └── scripts/
│       ├── download_data.py         # 数据集下载脚本
│       ├── preprocess_data.py       # 数据预处理脚本
│       └── import_data.py           # Neo4j 数据导入脚本
│
└── frontend/                  # Vue 3 前端
    ├── index.html
    ├── package.json           # 依赖（Vue 3, Element Plus, ECharts, Axios）
    ├── vite.config.js         # Vite 构建配置 & API 代理
    └── src/
        ├── App.vue            # 根组件
        ├── main.js            # 入口（挂载 Pinia, Router, Element Plus）
        ├── api/               # API 请求模块
        │   ├── request.js     # Axios 实例（baseURL, 拦截器, 超时）
        │   ├── user.js        # 用户 API
        │   ├── movie.js       # 电影 API
        │   ├── rating.js      # 评分 API
        │   ├── recommend.js   # 推荐 API
        │   ├── graph.js       # 知识图谱 API
        │   └── admin.js       # 管理员 API
        ├── components/
        │   └── MoviePoster.vue  # 彩色占位封面组件
        ├── layouts/
        │   ├── UserLayout.vue   # 用户端布局（顶部导航）
        │   └── AdminLayout.vue  # 管理端布局（侧边栏）
        ├── router/
        │   └── index.js         # 路由配置 & 守卫
        ├── stores/
        │   └── user.js          # 用户状态管理（Pinia）
        ├── utils/
        │   └── genre.js         # 电影类型解析工具
        └── views/
            ├── Login.vue          # 登录页
            ├── Register.vue       # 注册页
            ├── admin/             # 管理端页面
            │   ├── Dashboard.vue      # 数据概览仪表盘
            │   ├── UserManage.vue     # 用户管理
            │   ├── MovieManage.vue    # 电影管理
            │   ├── RatingManage.vue   # 评分管理
            │   ├── GraphManage.vue    # 知识图谱可视化
            │   └── ModelEvaluate.vue  # 模型评估（RMSE/MAE/覆盖率）
            └── user/              # 用户端页面
                ├── Home.vue           # 首页（推荐+热门）
                ├── MovieList.vue      # 电影列表（搜索/筛选/分页）
                ├── MovieDetail.vue    # 电影详情（评分/KG可视化/相似电影）
                ├── Recommend.vue      # 个性化推荐（4种算法切换）
                ├── MyRatings.vue      # 我的评分记录
                ├── KnowledgeGraph.vue # 知识图谱浏览
                └── Profile.vue        # 个人中心
```

---

## 三、核心技术栈

| 层级 | 技术 | 版本 | 作用 |
|------|------|------|------|
| 前端框架 | Vue 3 | 3.5+ | 组合式 API（`<script setup>`） |
| UI 组件库 | Element Plus | 2.13+ | 表格、表单、导航、反馈组件 |
| 图表 | ECharts | 6.0 | 知识图谱力导向图、评估图表 |
| 状态管理 | Pinia | 3.0+ | 用户登录状态 |
| 路由 | Vue Router | 5.0+ | 前端路由、导航守卫 |
| HTTP | Axios | 1.13+ | API 请求、拦截器、超时处理 |
| 构建工具 | Vite | 7.3+ | 开发服务器、热更新、API 代理 |
| 后端框架 | Flask | 3.0 | RESTful API 服务 |
| 图数据库 | Neo4j Community | 3.5.35 | 知识图谱存储与查询 |
| 认证 | PyJWT | 2.8 | JWT 令牌生成与校验 |
| 密码加密 | Werkzeug | 3.0 | PBKDF2 + SHA256 哈希 |
| 科学计算 | NumPy / SciPy / Pandas | - | 矩阵运算、稀疏矩阵、数据处理 |
| 机器学习 | scikit-learn | 1.3 | 余弦相似度、数据集划分 |
| 运行环境 | Java 8 (JDK 1.8) | - | Neo4j 3.5 运行时依赖 |
| 运行环境 | Python | 3.8+ | 后端运行时 |
| 运行环境 | Node.js | 20.19+ / 22.12+ | 前端构建运行时 |

---

## 四、知识图谱设计

### 4.1 图数据模型

```
(User) --[:RATED {rating, rating_normalized, date_str}]--> (Movie) --[:BELONGS_TO]--> (Genre)
```

**三类节点：**

| 节点标签 | 关键属性 | 说明 |
|---------|---------|------|
| `User` | userId, username, password_hash, role, created_at | 用户（训练集 + 注册用户） |
| `Movie` | movieId, title, clean_title, year, genres | 电影（9,742 部） |
| `Genre` | genreId, name | 类型（20 种） |

**两类关系：**

| 关系类型 | 方向 | 属性 | 说明 |
|---------|------|------|------|
| `RATED` | User → Movie | rating (0.5-5.0), rating_normalized (0-1), date_str | 评分关系（100,836+ 条） |
| `BELONGS_TO` | Movie → Genre | 无 | 类型归属（多对多，22,084 条） |

### 4.2 约束与索引

```cypher
-- 唯一性约束（确保 ID 不重复）
CREATE CONSTRAINT ON (u:User)  ASSERT u.userId  IS UNIQUE;
CREATE CONSTRAINT ON (m:Movie) ASSERT m.movieId IS UNIQUE;
CREATE CONSTRAINT ON (g:Genre) ASSERT g.genreId IS UNIQUE;

-- 辅助索引（加速常用查询）
CREATE INDEX ON :User(username);
CREATE INDEX ON :Movie(title);
CREATE INDEX ON :Genre(name);
```

---

## 五、推荐算法实现

### 5.1 基于用户的协同过滤（User-Based CF）

**原理：** 找到与目标用户评分行为最相似的 K 个邻居，用邻居的评分加权预测目标用户对未看电影的评分。

**实现流程：**
1. 加载 `ratings_cleaned.csv`，构建 **用户-电影稀疏评分矩阵**（610×9724）
2. 使用 `sklearn.cosine_similarity` 计算用户间余弦相似度矩阵
3. 对目标用户，取前 K=20 个最相似用户
4. 对每部未看电影，加权求和：$\hat{r}_{ui} = \frac{\sum_{v \in N(u)} sim(u,v) \cdot r_{vi}}{\sum_{v \in N(u)} |sim(u,v)|}$
5. 按预测评分排序，返回 Top-N 推荐

**新用户处理：** 当用户不在训练矩阵中（userId > 610），回退至知识图谱路径推荐 + 热门电影补充。

### 5.2 SVD 矩阵分解

**原理：** 对用户-电影评分矩阵进行奇异值分解，提取隐向量后重构完整评分矩阵，预测缺失评分。

**实现流程：**
1. 使用相同的用户-电影评分矩阵（去均值处理）
2. 调用 `scipy.sparse.linalg.svds` 进行 SVD 分解，保留 k=50 个奇异值
3. 重构预测矩阵：$\hat{R} = U \cdot \Sigma \cdot V^T + \mu$
4. 过滤用户已评分电影，按预测评分排序返回 Top-N

### 5.3 知识图谱路径推荐

**原理：** 利用 Neo4j 中的图结构，通过用户高评分电影的**类型路径**发现新电影。

**Cypher 查询：**
```cypher
MATCH (u:User)-[r:RATED]->(m1:Movie)-[:BELONGS_TO]->(g:Genre)<-[:BELONGS_TO]-(m2:Movie)
WHERE r.rating >= 3.5 AND NOT m2.movieId IN $ratedIds
WITH m2, count(DISTINCT g) AS pathScore, collect(DISTINCT g.name) AS viaGenres
...
ORDER BY pathScore DESC, avgRating DESC
```

**核心指标：** `pathScore`（共享类型数量越多，推荐优先级越高）

### 5.4 混合推荐

**原理：** 融合三种算法结果的加权融合推荐。

**权重分配：**
| 算法 | 权重 |
|------|------|
| 协同过滤 | 0.3 |
| SVD | 0.4 |
| 知识图谱 | 0.3 |

**融合流程：**
1. 分别调用三种算法获取 2×Top-N 候选
2. 各自归一化分数至 [0, 1]
3. 加权求和：$score_i = w_{cf} \cdot s_{cf} + w_{svd} \cdot s_{svd} + w_{kg} \cdot s_{kg}$
4. 统一排序取 Top-N，附带各算法贡献标签

### 5.5 模型评估

使用 80/20 训练测试集划分，评估指标：

| 指标 | 公式 | 说明 |
|------|------|------|
| RMSE | $\sqrt{\frac{1}{n}\sum(r-\hat{r})^2}$ | 均方根误差，越小越好 |
| MAE | $\frac{1}{n}\sum|r-\hat{r}|$ | 平均绝对误差，越小越好 |
| 覆盖率 | $\frac{|推荐电影集合|}{|全部电影|}×100\%$ | 推荐多样性 |

---

## 六、后端 API 接口

### 6.1 认证体系

- **加密方式：** Werkzeug PBKDF2 + SHA256
- **令牌格式：** JWT（有效期 24 小时）
- **传递方式：** HTTP Header `Authorization: Bearer <token>`
- **装饰器：** `@login_required`（解析用户信息）、`@admin_required`（需管理员角色）

### 6.2 主要接口

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| **用户** | POST | `/api/user/login` | 登录 |
| | POST | `/api/user/register` | 注册 |
| | GET | `/api/user/profile` | 获取个人信息 |
| **电影** | GET | `/api/movie/list` | 电影列表（分页/搜索/类型筛选） |
| | GET | `/api/movie/detail/<id>` | 电影详情 |
| | GET | `/api/movie/genres` | 类型列表 |
| | GET | `/api/movie/hot` | 热门电影 |
| | GET | `/api/movie/similar/<id>` | 相似电影（基于 KG） |
| **评分** | POST | `/api/rating/add` | 添加/更新评分 |
| | GET | `/api/rating/my` | 我的评分记录 |
| | GET | `/api/rating/status` | 查询对某电影的评分状态 |
| | DELETE | `/api/rating/delete` | 删除评分 |
| **推荐** | GET | `/api/recommend/collaborative` | 协同过滤推荐 |
| | GET | `/api/recommend/svd` | SVD 矩阵分解推荐 |
| | GET | `/api/recommend/knowledge` | 知识图谱路径推荐 |
| | GET | `/api/recommend/hybrid` | 混合推荐 |
| **图谱** | GET | `/api/graph/movie/<id>` | 电影知识图谱 |
| | GET | `/api/graph/global` | 全局知识图谱 |
| **管理** | GET | `/api/admin/dashboard` | 仪表盘统计数据 |
| | GET | `/api/admin/users` | 用户管理列表 |
| | GET | `/api/admin/ratings` | 评分管理列表 |
| | POST | `/api/admin/evaluate` | 模型评估（耗时较长） |

---

## 七、前端架构

### 7.1 路由设计

系统采用双布局设计：

- **UserLayout**（顶部导航）：用户端所有页面（`/home`, `/movies`, `/recommend` 等）
- **AdminLayout**（侧边栏）：管理端所有页面（`/admin/*`）

路由守卫处理：
- 未登录用户访问受保护页面 → 重定向至登录页
- 非管理员访问管理端 → 重定向至首页
- 已登录用户访问登录/注册页 → 重定向至主页

### 7.2 状态管理

使用 Pinia 管理用户登录状态：
- `token` → `localStorage` 持久化
- `userInfo`（userId, username, role）→ `localStorage` 持久化
- 登出时清除所有状态

### 7.3 API 请求

Axios 实例统一配置：
- `baseURL: /api`
- 请求拦截器：自动附加 `Authorization` 头
- 响应拦截器：401 状态自动清除 token 并跳转登录
- 全局超时：180 秒
- 模型评估接口独立超时：600 秒

### 7.4 知识图谱可视化

使用 ECharts `graph` 类型力导向图：
- **节点颜色：** 电影（#409EFF 蓝）、类型（#67C23A 绿）、用户（#E6A23C 橙）
- **交互：** 拖拽、缩放、悬停 tooltip
- **搜索：** 通过电影名称自动补全搜索
- **控制：** 电影/用户节点数量独立调节

---

## 八、业务工作流程

### 8.1 数据准备流程

```
MovieLens CSV  →  preprocess_data.py（清洗/划分/归一化）  →  processed/*.csv
processed/*.csv  →  import_data.py（MERGE 节点/关系）  →  Neo4j 知识图谱
```

### 8.2 用户使用流程

```
注册/登录 → 浏览电影列表 → 查看电影详情 → 评分
    ↓                                      ↓
查看知识图谱（探索电影关联） ←——————— 系统生成推荐
    ↓
访问"个性化推荐"页面（切换 4 种算法查看结果）
```

### 8.3 管理员工作流程

```
登录（admin/123456） → 数据概览仪表盘
    ↓
用户管理（查看/删除用户）
电影管理（浏览/搜索电影）
评分管理（查看/删除评分记录）
知识图谱（全局图谱可视化）
模型评估（运行评估 → 查看 RMSE/MAE/覆盖率对比图表）
```

### 8.4 推荐计算流程

```
用户请求推荐
    ↓
检查用户是否在训练矩阵中
    ├── 是：正常 CF/SVD 计算 → 补充电影详情 → 返回
    └── 否：回退推荐
              ├── 优先：知识图谱路径推荐（基于用户已有评分）
              └── 补充：热门电影填充（过滤已评分电影）
                    ↓
              补充电影详情（title, genres, avgRating）→ 返回
```
